<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cashly is entirely cloud-backed (Firebase Auth + Firestore), so it
         needs network access in every build variant, not just debug/profile
         (which only declare this for the Flutter tool's own hot-reload
         connection — see those manifests' comments). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="Cashly Lao"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false">
        <!-- Cashly's local Firestore persistence cache holds a real user's
             financial data on-disk. `allowBackup="false"` disables both
             Android Auto Backup (Google Drive) and device-to-device
             transfer at the OS level for this app entirely — the safest
             default for a financial app, and one every account already has
             a cloud copy of via Firebase, so nothing is actually lost by
             not also backing up the local cache. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Cashly always sends data-only FCM messages and displays them
             itself via flutter_local_notifications (see
             fcm_message_providers.dart / fcm_background_handler.dart), so
             this meta-data is never used by that path. It exists purely so
             a `notification`-payload message sent manually through the
             Firebase Console's Notification Composer (a promotional/
             broadcast use case this app deliberately leaves open, with no
             code of its own) still auto-displays with the right icon/color
             instead of a bare default. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/ic_launcher_background" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
